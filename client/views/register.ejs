<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>formulario</title>
    <link rel="stylesheet" href="/styleRegister.css">
</head>
<body>
    <form action="/users/register" method="post">
    <header class="header">
        <h1>Project</h1>
    </header>
     <div class="main-login">
        <div class="left-login">
            <h1>Faça login<br> E entre para o nosso time</h1>
            <img src="/img/img.svg" class="left-img" alt="">
        </div>
        <div class="right-login">
            <div class="card-login">
                <h1>REGISTER</h1>
                <div class="textfield">
                    <ul>
                       <% if(typeof erros != 'undefined') { %> <% erros.forEach(erros=>{ %>
                                 <li><%= erros.message %></li>
                          <% }) %> <% } %>
                    </ul>
                    <label for="usuario">Usuário</label>
                    <input type="email" name="email" id="email" placeholder="Usuário" required>
                </div>
                <div class="textfield">
                    <label for="senha">Senha</label>
                    <input type="password" name="password" id="password" placeholder="Senha" required>
                </div>
                <div class="textfield">
                    <label for="senha">Confirme Senha</label>
                    <input type="password" name="password2" id="password2" placeholder="Senha" required>
                </div>
                <button class="btn-login">Register</button>
                <a href="/users/login">Already Registered? Login here </a>
            </div>
        </div>
     </div>
    </form>
</body>
</html>